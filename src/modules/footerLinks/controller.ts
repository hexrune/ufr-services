import { Controller } from '../../types/module';

export const controller: Controller = [
    {
        method: 'GET',
        url: '/footer-links',
        handler: async () => {
            try {
                //
            } catch (e) {
                //
            }
        },
    },
];
